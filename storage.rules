rules_version = '2';
// Firebase Storage is no longer used; all file uploads go to Cloudinary.
// Rules retained as an empty placeholder so the deploy tooling doesn't error.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
